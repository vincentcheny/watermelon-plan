<!--pages/reward/index.wxml-->
<view class="integral">🍭：{{userIntegral || 0}}</view>
<view>
    <list bind:submitFunc="submit" types="{{type}}" records="{{records}}" titles="{{titles}}" name_width='70%' content_width='30%'></list>
</view>