<!--pages/reward/index.wxml-->
<view class="integral">🍭：{{userIntegral || 0}}</view>
<view style="width: 99%;">
    <list bind:submitFunc="submit" types="{{type}}" records="{{records}}" titles="{{titles}}" theme="{{theme}}" name_width='70%' content_width='30%'></list>
</view>